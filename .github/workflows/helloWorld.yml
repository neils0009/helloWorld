name: helloWorld
on: push
jobs:
  helloWorld:
    runs-on: ubuntu-latest
    steps:
      - name: first-step
        run: echo "Hello World"
      - name: second-step
        run: echo " this is from actions"
